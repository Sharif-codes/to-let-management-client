import { APIProvider } from '@vis.gl/react-google-maps';
import React from 'react';

const Map = () => {
    return (
        <APIProvider>
            <div>
                
            </div>
        </APIProvider>
    );
};

export default Map;